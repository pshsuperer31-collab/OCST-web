/**
 * 문항 카피 개정 + 상괘 서술 오류 수정
 *
 * index.html의 [LOWER · UPPER · QL · QU] 구간을 통째로 교체한다.
 * 구간 = '/* 하괘 = 나의 기반' 부터 'const STAGES' 직전까지.
 *
 * 수정한 오류 2건 (상괘 표기는 [上,五,四] 순인데 서술이 어긋나 있었다):
 *   '001' 震 = 四만 陽  → "결정권자만 움직였다"(五 기준)로 잘못 서술돼 있었음
 *   '110' 巽 = 四만 陰  → "순풍인데 출구가 닫혔다"로 정반대로 서술돼 있었음
 */
import fs from 'fs';

const PATH = 'index.html';
const BLOCK = `/* 하괘 = 내가 딛고 선 쪽 — 初(마음의 불) · 二(손에 쥔 것) · 三(드러난 움직임) */
const LOWER = {
 '111':{t:'셋 다 서 있다 — 전력으로 밀고 있다',d:'마음도 가고, 지금 가진 것으로 되고, 이미 밖으로 움직이고 있습니다. 못 할 이유가 없는 상태라 위험은 바깥이 아니라 과속에서 옵니다.'},
 '011':{t:'채비는 됐는데 아직 안 나섰다',d:'마음도 가고 손에 쥔 것도 되는데, 밖으로는 아직 티가 안 납니다. 서두르지 않아 오히려 여유가 있는 자리예요.'},
 '101':{t:'겉은 도는데 속이 비었다',d:'마음도 가고 움직이고도 있는데, 정작 받쳐줄 것이 얇습니다. 離의 中虛 — 잘 보이는 것이 곧 튼튼한 것은 아닙니다.'},
 '001':{t:'마음만 치밀고 아직 아무것도',d:'자꾸 떠오르기는 하는데 쥔 것도 없고 움직이지도 않았습니다. 시작 직전에 눌려 있는 압력이에요.'},
 '110':{t:'뿌리 없이 따라 들어간다',d:'쥔 것도 있고 움직이고도 있는데, 정작 내 마음이 가 있지 않습니다. 남의 흐름에 실려 있는 상태예요.'},
 '010':{t:'여력은 있는데 발이 안 떨어진다',d:'가진 것은 되는데 마음도 안 가고 움직이지도 못합니다. 坎의 中滿 — 속은 찼으나 빠져 있는 자리입니다.'},
 '100':{t:'맨몸으로 나섰다가 벽을 만났다',d:'마음도 식었고 쥔 것도 없는데 몸만 먼저 나갔습니다. 위가 단단히 막혀 멈춰야 하는 자리예요.'},
 '000':{t:'내려놓고 받아들이는 중',d:'마음도 안 가고 쥔 것도 없고 움직이지도 않습니다. 그러나 비어 있어야 담깁니다 — 버티는 것이 일인 자리예요.'}
};
/* 상괘 = 내가 통제 못 하는 쪽 — 표기는 [上, 五, 四] 순.
   四(판의 바람) · 五(열쇠 쥔 곳) · 上(끝의 문) */
const UPPER = {
 '111':{t:'판이 활짝 열려 있다',d:'때도 괜찮고 열쇠 쥔 쪽도 반응하고 끝 그림도 섭니다. 이런 판은 오래가지 않으니 지금이 시점입니다.'},
 '011':{t:'다 좋은데 끝이 안 보인다',d:'때도 되고 열쇠 쥔 쪽도 움직이는데, 정작 어떻게 끝날지가 그려지지 않습니다. 좋게 흘러가다 마무리에서 새는 형국이에요.'},
 '101':{t:'명분은 섰는데 열쇠가 안 돈다',d:'때도 되고 끝 그림도 서는데 열쇠 쥔 쪽만 미지근합니다. 화려한 판, 빈 가운데.'},
 '001':{t:'판만 술렁이고 아직 아무것도',d:'때는 나쁘지 않은데 열쇠 쥔 쪽은 미지근하고 끝도 안 보입니다. 우레처럼 기척만 먼저 온 자리예요.'},
 '110':{t:'때는 아닌데 위쪽 이야기는 됐다',d:'분위기·여건은 어수선한데, 열쇠 쥔 쪽은 반응하고 끝 그림도 섭니다. 시류를 타는 게 아니라 스며들어 가는 판입니다.'},
 '010':{t:'험한 판, 열쇠 쥔 쪽만 밀고 온다',d:'때도 아니고 끝도 안 보이는데 상대는 밀고 들어옵니다. 가장 조심할 배치입니다.'},
 '100':{t:'다 멎었는데 문 하나가 남았다',d:'때도 아니고 열쇠 쥔 쪽도 미지근한데, 끝나는 그림 하나는 보입니다. 산이 앞을 막고 길만 트인 자리예요.'},
 '000':{t:'판 전체가 조용하다',d:'아무도 밀어주지 않고 아무도 막지도 않습니다. 내가 만들지 않으면 아무 일도 없습니다.'}
};

const QL = [
 {y:'初爻 · 마음의 불',t:'이 일이 자꾸 떠올라서, 안 하고 넘기면 답답한가요?',
  a:'자꾸 떠오른다', b:'해야 하는 줄은 아는데 마음이 안 간다',
  h:'의욕의 크기가 아니라 끌림의 유무입니다. 부담스러워도 계속 생각난다면 앞쪽이에요.'},
 {y:'二爻 · 손에 쥔 것',t:'지금 가진 것(돈·시간·실력·사람)으로 이 일을 시작할 수 있나요?',
  a:'지금 것으로도 된다', b:'더 갖춰야 한다',
  h:'완벽한 준비가 아니라 착수선을 묻습니다. 모자라도 뗄 수는 있으면 앞쪽이에요.'},
 {y:'三爻 · 드러난 움직임',t:'남이 봤을 때, 내가 이 일을 이미 시작한 것으로 보이나요?',
  a:'움직이는 게 보인다', b:'아직 티가 안 난다',
  h:'마음속 결심은 빼고, 밖으로 나간 행동만 세어주세요.'}
];
const QU = [
 {y:'四爻 · 판의 바람',t:'요즘 분위기·여건이 이런 일을 하기에 괜찮은 편인가요?',
  a:'때가 나쁘지 않다', b:'때가 아니거나 어수선하다',
  h:'내 사정 말고 바깥 형편입니다. 업계·시기·주변 상황이요.'},
 {y:'五爻 · 열쇠 쥔 곳',t:'이 일의 승인·수요·허락을 쥔 쪽이 있고, 그쪽이 움직이고 있나요?',
  a:'그쪽이 반응하고 있다', b:'미지근하거나, 그런 상대가 없다',
  h:'상대·윗선·손님·시장 중 이 건의 열쇠를 쥔 한 곳만 보세요. 그런 곳이 없으면 뒤쪽입니다.'},
 {y:'上爻 · 끝의 문',t:'이 일이 어떻게 끝날지, 끝 그림이 그려지나요?',
  a:'끝이 보인다', b:'끝이 안 보인다',
  h:'좋게 끝날지가 아니라, 끝나는 모양이 그려지는지를 묻습니다.'}
];
`;

let h = fs.readFileSync(PATH, 'utf8');
const start = h.indexOf('/* 하괘 = ');
const end = h.indexOf('const STAGES');
if (start < 0 || end < 0 || end < start) { console.error('MARKER FAIL', start, end); process.exit(1); }
h = h.slice(0, start) + BLOCK + h.slice(end);

/* 문항 렌더러에 힌트 줄 추가 */
const OLD_RENDER = `    return '<div class="q"><div class="yao">'+q.y+'</div><div class="txt">'+q.t+'</div>'+
     '<div class="opts" data-i="'+(off+k)+'">'+
     '<button data-v="1">'+q.a+'</button><button data-v="0">'+q.b+'</button></div></div>';`;
const NEW_RENDER = `    return '<div class="q"><div class="yao">'+q.y+'</div><div class="txt">'+q.t+'</div>'+
     '<div class="opts" data-i="'+(off+k)+'">'+
     '<button data-v="1">'+q.a+'</button><button data-v="0">'+q.b+'</button></div>'+
     '<div class="hint">'+q.h+'</div></div>';`;
if (!h.includes(OLD_RENDER)) { console.error('RENDER MARKER FAIL'); process.exit(1); }
h = h.replace(OLD_RENDER, NEW_RENDER);

/* 힌트 스타일 */
const OLD_CSS = `.opts{display:flex;gap:8px;flex-wrap:wrap}`;
h = h.replace(OLD_CSS, `.opts{display:flex;gap:8px;flex-wrap:wrap}
.hint{font-size:12.5px;color:var(--mute);margin-top:8px;padding-left:2px}`);

/* 선택지 문장이 길어져 최소폭을 넓힌다 */
h = h.replace('.opts button{flex:1;min-width:130px;padding:11px 10px;',
              '.opts button{flex:1;min-width:180px;padding:11px 12px;');

fs.writeFileSync(PATH, h);
console.log('카피 개정 완료 — 총', h.length, 'bytes');
